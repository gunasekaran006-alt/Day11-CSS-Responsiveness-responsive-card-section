# Responsive Product Gallery 🛒

A clean and modern product card layout designed for e-commerce interfaces. This project demonstrates responsive web design principles using a **mobile-first** strategy.


## ✨ Key Features
- **Responsive Layout**: Automatically adjusts from 1 to 3 columns using **CSS Grid** (`min-width` media queries).
- **Mobile-First Design**: Optimized for smaller screens first, ensuring performance and usability.
- **Modern UI/UX**: Includes stylized buttons, card elevations, and consistent typography.
- **Image Optimization**: Uses `object-fit: cover` to maintain aspect ratios regardless of image dimensions.

## 🛠️ Built With
- **HTML5**: Semantic markup for better SEO and accessibility.
- **CSS3**: Custom properties, Flexbox for button alignment, and Grid for the main layout.

## 📂 Project Structure
```text
├── index.html   # Main HTML structure
└── style.css    # Styling and responsive breakpoints
